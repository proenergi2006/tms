# Base URL backend TMS (Laravel) — lihat Design Document Bagian 3.1
VITE_API_BASE_URL=http://localhost:8000/api/v1

# URL SYOP v3 — dipakai tombol "Kembali ke SYOP" di topbar (DefaultLayout.vue),
# cuma tampil untuk sesi yang login lewat SSO (lihat stores/auth.js ssoOrigin).
VITE_SYOP_URL=https://syop.proenergi.co.id
